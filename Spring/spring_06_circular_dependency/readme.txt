循环依赖：
    依赖指的是一个对象跟另外一个对象构成关系
    循环依赖指的是：两个依赖之间都构成关系，A对象与B对象构成关系，B对象又与A对象之间构成关系
现在要研究循环依赖这个问题对我们bean对象的创建和使用会不会造成影响？
    因为husbandBean对象在Spring容器中只有一个，后续在wifeBean对象属性赋值的能正确拿到
    如果是多例，会有多个同名的bean对象，spring容器不知道用哪个