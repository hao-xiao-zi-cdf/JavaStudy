spring:
  application:
    name: service-product
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        import-check:
          enabled: false
    sentinel:
      transport:
        # 配置sentinel控制台地址
        dashboard: localhost:8080
      # 项目已启动就加载sentinel规则
      eager: true
server:
  port: 9000
